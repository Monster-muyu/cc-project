# 工作约束

> 本文件定义本项目中主 Agent 的工作方式，所有任务执行均须遵守。

## 1. 任务分配原则

### 复杂任务 — 主 Agent 自己做

涉及以下情况的任务，由主 Agent 亲自完成，**不**委派给子 Agent：

- 需要跨多文件理解架构、上下文密集的设计决策
- 涉及安全、权限、破坏性操作（删除、覆盖、发布）
- 需要与用户确认方案、做权衡判断
- 浏览器自动化操作（Browser Use 类）
- 长链路、多步骤、状态互相依赖的任务
- 需要对外输出或难以撤销的操作

### 简单任务 — 委派给子 Agent

满足以下特征的独立任务，可委派给子 Agent：

- 单一目标、范围清晰、输入输出明确
- 不依赖主对话上下文即可独立完成
- 可并行执行、互不阻塞
- 示例：文件内容查找、单一函数实现、文档片段生成、格式化整理、单点信息查询

## 2. 子 Agent 数量上限

**同时存活的子 Agent 最多 3 个。**

- 新开子 Agent 前先检查当前已开数量
- 达到 3 个时，等待其中某个完成后再开新的
- 若有多个独立任务，可在一次响应中并发派发（合并到单条消息中）

## 3. 执行检查清单

每次接到任务时，主 Agent 按以下顺序判断：

1. [ ] 是否复杂任务？是 → 自己做，结束。
2. [ ] 是否可拆为多个独立简单子任务？是 → 判断是否需要并行。
3. [ ] 当前子 Agent 数量是否 < 3？是 → 派发；否 → 等待。

## 4. 备注

- 子 Agent 的最终结果会回传给主 Agent，**不**直接展示给用户，主 Agent 需整合后再回复。
- 子 Agent 每次启动都是全新的，派发时 prompt 必须自包含、不依赖隐性上下文。
